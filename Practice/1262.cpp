#include<iostream>
#include<cstdio>
#include<cstring>
#include<algorithm>

using namespace std;

const int N = 110;
int p[N], d[N], t[N];
int n;
int t;
int goWhere(int cur){

    int target = -1;
    for (int i = 1; i < cur;++i){
        if(p[cur]<)
    }
}
int main(){
    scanf("%d", &n);
    for (int i = 1; i <= n;++i){
        scanf("%d", &p[i]);
    }
    for (int i = 1; i <= n;++i){
        scanf("%d", &d[i]);
    }
    for (int i = 1; i <= n - 1;++i){
        scanf("%d", &t[i]);
    }
    scanf("%d", &t);
    
}